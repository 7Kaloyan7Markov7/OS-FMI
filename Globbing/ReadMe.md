to do
1) List all .txt files
2) List files starting with lowercase letters only
3) List files starting with uppercase letters only
4) Match files that are exactly one character long
5) Match files that are exactly two characters long
6) Match files starting with a, b, or z
7) Match files NOT starting with lowercase letters
8) Match files ending with .csv
9) Match files that start with test and end with exactly one digit
10) Match files that start with test and end with exactly three digits
11) Match log-YYYY-MM.log only for year 2024
12) Match log-2024-??.log where month is any two digits
13) Match images with uppercase extension .JPG only
14) Match images where the numeric part is exactly three digits
15) Match files that contain no digits at all
16) Match files that do NOT contain lowercase letters anywhere
17) Match files that do NOT end in .log
18) Match hidden files only
19) Match hidden files that are exactly 4 characters long (including the dot)
20) Match files that: start with uppercase, contain an underscore, end with .jpg or .JPG
